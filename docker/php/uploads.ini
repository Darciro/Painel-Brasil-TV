; Raises PHP upload/memory limits above the stock WordPress image defaults
; so the theme's media uploads (video thumbnails, images) aren't rejected.
file_uploads = On
memory_limit = 256M
upload_max_filesize = 64M
post_max_size = 64M
max_execution_time = 300
